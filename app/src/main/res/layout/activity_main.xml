<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Lottie background animation -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/lottieBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="false"
        android:focusable="false"
        android:scaleType="fitEnd"
        app:lottie_autoPlay="true"
        app:lottie_fileName="background.json"
        app:lottie_loop="true" />

    <!-- Foreground layout with buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp"
        android:background="#80000000">

        <Button
            android:id="@+id/buttonDestinations"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/explore_destinations2"
            android:textSize="18sp"
            android:layout_marginBottom="16dp"
            android:backgroundTint="#FFFFFF"
            android:textColor="#000000" />

        <Button
            android:id="@+id/buttonViewTrips"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/view_saved_trips2"
            android:textSize="18sp"
            android:layout_marginBottom="16dp"
            android:backgroundTint="#FFFFFF"
            android:textColor="#000000" />

        <Button
            android:id="@+id/buttonChat"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/ai_chat_assistant1"
            android:textSize="18sp"
            android:backgroundTint="#FFFFFF"
            android:textColor="#000000" />
    </LinearLayout>
</FrameLayout>
